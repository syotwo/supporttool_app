<% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>

<div class="item_list_new">
    <div class="wrap_formhead">
        <p>気分転換メモ の作成</p>   
    </div>
    <p>＊気分転換をメモしておくことで、ストレスを感じた時にすぐに対処することができます。</p>
    <%= render 'layouts/form_itemlist' %>    
</div>

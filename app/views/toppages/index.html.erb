
<% if logged_in? %>
  <div class= user-name>
    <p>Welcome back: <%= current_user.name %></p>
  </div>

  <div class= toppage>    
    <ul>
      <% @item_lists.each do |item_list| %>
        <li>
            <%= link_to item_list.list_name , item_list_path(item_list) %>
            <p class= item_list_products>view</p>
            <% if item_list.image? %>
              <%= image_tag item_list.image.url %> 
            <% else %>
              <%= image_tag "/assets/default.jpg" %>
            <% end %>
        </li>
      <% end %>
    </ul>
  </div>  
  
<% else %>
  <div class= logout>
    <p>Welcome to Mind Hacks</p>
    <h1>あなたの思考を解き放つためのコミュニティー<br>それがMind Hacksです</h1>
    <%= image_tag "/assets/innovation.jpg" %>
    <p>全く新しいあなたの思考を解き放つ体験</p>
    <p>あなたの思考を補助するツール、セレンティビティ</p>
    <%= link_to 'Sign up now!', signup_path %>
  </div>
  <div class= toppage>    
    <ul>
      <% @item_lists.each do |item_list| %>
        <li>
            <%= link_to item_list.list_name , item_list_path(item_list) %>
            <p class= item_list_products>view</p>
            <% if item_list.image? %>
              <%= image_tag item_list.image.url %> 
            <% else %>
              <%= image_tag "/assets/default.jpg" %>
            <% end %>
        </li>
      <% end %>
    </ul>
  </div>  
<% end %>


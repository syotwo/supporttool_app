<h1>Tool</h1>

<% if @item.img? %>
    <%= image_tag @item.img.url %>
    <p><%= @item.item_name %></p>
<% else %>

<% end %>

<%= link_to 'Cancel', items_path %>
<%= link_to 'Edit Tool', edit_item_path(@item) %>
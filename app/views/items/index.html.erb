<h1>Tool一覧</h1>

<% @items.each do |item| %>
    <div>
        <p>
            <%= link_to item.id, item_path(item) %>
            <%= image_tag item.img.url %>
            <br>
            <%= item.item_name %>
        </p>
    </div>
<% end %>